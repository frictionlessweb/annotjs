{
  "scripts": {
    "start": "npm run dev --prefix docs",
    "build": "npm run build --prefix docs",
    "test": "npm test --prefix lib && npm test --prefix docs",
    "setup": "npm install"
  },
  "workspaces": [
    "docs",
    "lib"
  ]
}
